version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: genai-app
    env_file:
      - ./backend/.env
    environment:
      NODE_ENV: production
    ports:
      - "3000:3000"
    restart: unless-stopped
